@page "/"

<h1>Hello, world!</h1>

Welcome to your new app.

<TextEdit @ref="@textEditRef" />
<Button Color="Color.Primary" Clicked="async ()=> { await textEditRef.FocusAsync(); }">Focus</Button>

@code {
  private TextEdit textEditRef;

  protected override async Task OnAfterRenderAsync(bool firstRender)
  {
    if (firstRender)
    {
      await textEditRef.FocusAsync();
    }
  }

  //protected override Task OnAfterRenderAsync(bool firstRender)
  //{
  //  if (firstRender)
  //  {
  //    textEditRef.Focus();
  //  }

  //  return Task.CompletedTask;
  //}
}
